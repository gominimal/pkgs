[stdlib]
minimum_version = "0.0.8"

[tasks.shell]
packages = ["base", "bash", "claude-code"]
exec = "bash --noprofile -l"

[tasks.claude]
packages = ["base", "bash", "claude-code"]
exec = "claude --dangerously-skip-permissions"
