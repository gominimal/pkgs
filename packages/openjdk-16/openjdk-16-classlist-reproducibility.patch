--- a/make/GenerateLinkOptData.gmk	2026-09-23 03:23:24
+++ b/make/GenerateLinkOptData.gmk	2026-09-23 03:23:24
@@ -88,7 +88,8 @@
 	        $(CAT) $(LINK_OPT_DIR)/stderr $(JLI_TRACE_FILE) ; \
 	        exit $$exitcode \
 	    )
-	$(GREP) -v HelloClasslist $@.raw.2 > $@
+	$(GREP) -v HelloClasslist $@.raw.2 > $@.unsorted
+	( { $(GREP) '^#' $@.unsorted || true; }; { $(GREP) -v '^[#@]' $@.unsorted || true; } | LC_ALL=C $(SORT); { $(GREP) '^@' $@.unsorted || true; } | LC_ALL=C $(SORT) ) > $@
 
 # The jli trace is created by the same recipe as classlist. By declaring these
 # dependencies, make will correctly rebuild both jli trace and classlist
